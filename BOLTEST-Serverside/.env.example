# Server settings
PORT=5000
NODE_ENV=development
JWT_SECRET=changeme_secure_random
ALLOWED_ORIGINS=http://localhost:3000
AZURE_API_VERSION=5.0

# Azure DevOps / TFS settings (on-prem)
AZDO_ORG_URL=
AZDO_PROJECT=Epos
AUTH_MODE=PAT
AZDO_PAT=

# Optional: protect /api/ado/* backend-only routes
BACKEND_API_KEY=

# Encryption key for PAT (base64-encoded 32 bytes). Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ENCRYPTION_KEY=


